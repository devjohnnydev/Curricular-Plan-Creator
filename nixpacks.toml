[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install -g pnpm@latest", "pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @workspace/plano-ensino run build:prod"]

[start]
cmd = "node artifacts/plano-ensino/server.js"
